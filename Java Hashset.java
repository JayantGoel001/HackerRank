
        HashSet<String> h1=new HashSet<String>();
        for(int i=0;i<t;i++)
        {
            h1.add("("+pair_left[i]+","+pair_right[i]+")");
            System.out.println(h1.size());
        }
